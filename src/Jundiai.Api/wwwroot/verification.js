const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const token=()=>localStorage.getItem('jundiai.session');async function api(url,options={}){const t=token();const r=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(t?{Authorization:`Bearer ${t}`}:{'X-Demo-Role':'poc_admin','X-Demo-User':'poc.operador'}),...(options.headers||{})}});const b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.detail||b.title||`HTTP ${r.status}`);return b;}function fail(e){$('#error').hidden=false;$('#error').textContent=e.message;}
function render(d){$('#score-value').textContent=d.overallScore;$('#passed').textContent=`${d.passedBlocks}/${d.totalBlocks} blocos`;$('#score').style.setProperty('--score',`${d.overallScore}%`);$('#summary').innerHTML=`<strong>${esc(d.status)}</strong><p>Contract Pack ${esc(d.contractPack)} · gerado ${new Date(d.generatedAt).toLocaleString('pt-BR')}</p>`;$('#blocks').innerHTML=d.blocks.map(b=>`<article class="block ${b.passed?'pass':'fail'}"><div class="head"><div><strong>${String(b.block).padStart(2,'0')} · ${esc(b.name)}</strong><div class="muted">${esc(b.evidence)}</div></div><span class="score-badge">${b.score}</span></div><div class="routes">${b.evidenceRoutes.map(r=>`<a href="${esc(r)}" target="_blank">${esc(r)}</a>`).join('')}</div></article>`).join('');$('#disclaimers').innerHTML='<strong>Limites da verificação</strong><ul>'+d.disclaimers.map(x=>`<li>${esc(x)}</li>`).join('')+'</ul>';$('#error').hidden=true;}
async function latest(){try{const d=await api('/api/poc/verification/latest');render(d);}catch(e){if(String(e.message).includes('404'))return;fail(e)}}
$('#run').onclick=async()=>{try{$('#run').disabled=true;$('#run').textContent='Verificando…';const d=await api('/api/poc/verification/run',{method:'POST',body:'{}'});render(d);}catch(e){fail(e)}finally{$('#run').disabled=false;$('#run').textContent='Executar verificação completa'}};latest();